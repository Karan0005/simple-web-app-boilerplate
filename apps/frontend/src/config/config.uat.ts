import { EnvironmentEnum } from '@full-stack-project/shared';

export const CONFIG = {
    environment: EnvironmentEnum.UAT,
    production: false,
    apiUrl: 'http://uat.domain.com/api',
    logLevel: 'debug',
    enableDebugTools: true
};
